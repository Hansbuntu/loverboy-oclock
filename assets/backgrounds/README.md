Optional level backgrounds, wide pixel-art panels, cover-fitted to the 640x400 stage with a slow pan:

level-01.webp ... level-07.webp (cropped from assets/source/background-sheet.png, converted to WebP so they load fast)

If a file is missing, that level uses its built-in canvas silhouette scene.
Change paths/extensions per level in js/config.js (`bg`).
